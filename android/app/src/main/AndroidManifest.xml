<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Our FirebaseMessagingService displays notifications when the app
             is in the foreground (FCM hands the message to the app instead
             of drawing it natively in that state). Declaring it at the app
             level means Android routes messaging events here rather than to
             Capacitor's plugin default, which is a no-op in remote mode
             since it only emits to the JS bridge. -->
        <service
            android:name=".BikerOrNotMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 13+ (API 33+) requires runtime permission before notifications
         can be posted. Silently granted on older versions. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- navigator.geolocation inside the WebView. FINE for GPS (used by
         the check-in picker's "Use current location" bias); COARSE as a
         fallback so network-location-only devices still work. WebView
         additionally needs onGeolocationPermissionsShowPrompt in the
         chrome client — without that override the page-level request
         gets auto-denied even when the OS permission is granted. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-feature android:name="android.hardware.location" android:required="false" />

    <!-- Android 11+ package visibility for Custom Tabs. Without this,
         CustomTabsClient.getPackageName() returns null and we cannot pin
         the browser, letting URL-intercepting apps (Facebook, YouTube,
         Instagram) hijack external-link taps. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="androidx.browser.customtabs.action.CustomTabsService" />
        </intent>
    </queries>
</manifest>
