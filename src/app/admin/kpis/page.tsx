export const dynamic = 'force-dynamic'
export const metadata = { title: 'KPIs — BikerOrNot Admin' }

import KpisClient from './KpisClient'

export default function KpisPage() {
  return <KpisClient />
}
