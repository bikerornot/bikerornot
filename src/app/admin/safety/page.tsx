export const dynamic = 'force-dynamic'
export const metadata = { title: 'Safety Center — BikerOrNot Admin' }

import SafetyClient from './SafetyClient'

export default function SafetyPage() {
  return <SafetyClient />
}
